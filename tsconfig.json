{
  "compilerOptions": {
    "target": "es6", // Phiên bản JavaScript mà TypeScript sẽ biên dịch xuống (ES5, ES6, ES2015,...)
    "module": "commonjs", // Hệ thống module dùng trong dự án (commonjs cho Node.js)
    "esModuleInterop": true, // Cho phép import các module ES6 trong Node.js (require/exports)
    "outDir": "./dist", // Thư mục đầu ra cho các file biên dịch
    "rootDir": "./src", // Thư mục chứa các file TypeScript gốc
    "moduleResolution": "node", // Kiểu module resolution, dùng 'node' cho môi trường Node.js
    "skipLibCheck": true, // Bỏ qua kiểm tra kiểu cho các file định nghĩa thư viện .d.ts
    "sourceMap": true, // Tạo source map giúp debug dễ dàng hơn
    "experimentalDecorators": true, // Kích hoạt decorators (bắt buộc để dùng decorators)
    "emitDecoratorMetadata": true // Tạo metadata cho decorators (hữu ích khi dùng với frameworks như Angular)
  },
  "lib": ["es2015"],
  "include": ["src/."], // Các file được bao gồm trong quá trình biên dịch
  "exclude": ["node_modules", "dist"] // Loại trừ các thư mục không cần biên dịch
}
